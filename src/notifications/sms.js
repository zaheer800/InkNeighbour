/**
 * SMS notification module — Phase 3 stub (Twilio).
 * NOT active in Phase 1 or 2. Do not implement until explicitly instructed.
 */

export function sendSMS() {
  throw new Error('SMS notifications via Twilio are not active until Phase 3.')
}
