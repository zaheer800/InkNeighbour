/**
 * Stripe payment module — Phase 2 stub.
 * NOT active in Phase 1. Do not implement until explicitly instructed.
 *
 * Placeholder to maintain consistent payment router interface.
 */

export function getStripePaymentData() {
  throw new Error('Stripe payments are not active in Phase 1.')
}
